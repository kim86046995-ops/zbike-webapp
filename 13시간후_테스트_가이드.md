# 📢 13시간 후 테스트 가이드 (간단 버전)

## 🎯 목표
차용증 PDF 다운로드가 **완전한 내용**(약관, 서명, 신분증 포함)으로 생성되도록 수정 완료

---

## ⚡ 빠른 테스트 (5분)

### 1단계: 브라우저 준비
```
✅ 모든 Chrome 탭 닫기
✅ 새 시크릿 창 열기 (Ctrl+Shift+N)
```

### 2단계: 테스트 실행
1. **URL 접속**: https://3000-ikdibnajukplmgra2ipiv-2b54fc91.sandbox.novita.ai/static/loans
2. **F12 눌러서 Console 열기**
3. "김상춘" 차용증 찾기 (LOAN-20260201-0002)
4. "상세보기" 클릭
5. "PDF 다운로드" 클릭

### 3단계: 결과 확인

#### ✅ 성공 시 콘솔 로그:
```
🎯 [차용증] downloadLoanPDF 호출됨
🚀 [차용증] PDF 생성기 시작
📊 [차용증] 데이터: LOAN-20260201-0002
✅ [차용증] Blob 생성 완료: 150000 bytes (100KB 이상)
✅ [차용증] PDF 다운로드 완료
```

#### ❌ 실패 시 콘솔 로그:
```
✅ PDF 저장 완료 (File System Access API)  ← 이건 계약서꺼!
```

---

## 🔧 실패 시 해결 방법

### 방법 1: 캐시 완전 삭제
```
1. Ctrl+Shift+Delete
2. "캐시된 이미지 및 파일" 체크
3. "데이터 삭제"
4. 브라우저 완전히 닫기
5. 다시 시작
```

### 방법 2: 강제 새로고침
```
페이지에서 Ctrl+Shift+R
```

### 방법 3: 콘솔에서 직접 실행
```javascript
// F12 → Console에 붙여넣기
(async () => {
    if (!currentLoan) {
        alert('상세보기를 먼저 열어주세요');
        return;
    }
    await LoanPDFGenerator.generate(currentLoan);
})();
```

---

## 📊 Blob 크기로 판단

| Blob 크기 | 상태 | 설명 |
|----------|------|------|
| **30KB 이하** | ❌ 백지 | 이미지 누락 |
| **100KB ~ 500KB** | ✅ 정상 | 이미지 포함 |
| **500KB 이상** | ✅ 고품질 | 이미지 많음 |

---

## 🆘 긴급 대안

### 인쇄로 PDF 저장
```
1. 상세보기 열기
2. Ctrl+P
3. 대상: "PDF로 저장"
4. 저장
```

**참고**: 4번 반복 문제는 해결됨 (테스트 필요)

---

## 📞 추가 지원 필요 시

### 서버 로그 확인
```bash
cd /home/user/webapp
pm2 logs webapp --nostream --lines 50
```

### 데이터 확인
```bash
curl -s http://localhost:3000/api/loan-contracts/2 | jq '{
  loan_number,
  borrower_name,
  sig_len: (.borrower_signature | length),
  id_len: (.borrower_id_card_photo | length)
}'
```

---

## ✨ 주요 변경 사항

1. ✅ **독립 PDF 생성기**: `LoanPDFGenerator` 객체로 contracts.html과 완전 분리
2. ✅ **로그 구분**: `[차용증]` 접두사로 명확한 추적
3. ✅ **인쇄 수정**: `pdfContainer` 완전 숨김으로 4번 반복 해결
4. ✅ **함수 이름 변경**: `downloadLoanPDF()` 등 차용증 전용 이름 사용

---

**작성 시각**: 2026-02-01 오전  
**테스트 예정**: 13시간 후  
**현재 상태**: ✅ 수정 완료 (테스트 대기)

---

**중요**: `/static/contracts` 페이지는 절대 열지 마세요! (함수 충돌 방지)
